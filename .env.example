# Base URL for a real backend. When set, all /api/* requests are sent here
# instead of being intercepted by the in-browser MSW mock layer.
# Leave empty to use the mock API (default for local development).
VITE_API_BASE_URL=
