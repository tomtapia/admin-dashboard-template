[tool.commitizen]
version_provider = "npm"
version_scheme = "semver"
tag_format = "v$version"
changelog_file = "CHANGELOG.md"
changelog_increment_if_possible = true
annotated_tag = true
bump_message = "chore(release): $current_version → $new_version"
